apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tenant-certmanager

resources:
  - ../cert-manager
  - myminio-certificate.yaml
  - ../base

patchesStrategicMerge:
  - tenant.yaml
